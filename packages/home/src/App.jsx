import React from 'react';
import { IconButton } from '@little-rock/components';

export default () => (
  <div>
    <h1>Home Page...</h1>
    <IconButton label='Primary Icon Button' onClick={() => alert('Primary icon button is clicked')} />
  </div>
);
