import React from 'react';
import { Button } from '@little-rock/components';

export default () => (
  <div>
    <h1>Login Page</h1>
    <Button label="Primary Button" onClick={() => alert('Primary button is clicked')}/>
  </div>
);
